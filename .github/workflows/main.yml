name: ci
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: git status
      - run: git branch
      - run: docker build --target deploy -t gpc-lesson-community-site_rails:latest .